v0.1.0, 2014-02-21 -- Initial Release, refactored version of my poorly
                      executed TSGeom package.
v0.1.1, 2014-02-25 -- Change README.txt to README.rst so that Github will 
                      process it correctly.
v0.1.2, 2014-03-20 -- FIX bug that caused install error due to changed README
                      file name. 
v0.1.3, 2014-03-20 -- FIX of previous FIX. Apparently, I don't understand the 
                      README.rst usage pattern.
v0.1.4, 2017-05-09 -- Refactor for Cython instead of numba
v0.3.0, 2022-07-31 -- Switch to poetry for package management. Drop support
                      for Python below 3.8. Add plotting package helpers.
                      Add support for Pandas.
v0.3.3, 2025-09-12 -- roll back to basic Python ( cPython under the hood, expect 
                      same performance), standard setuptools+wheel package 
                      installation. Added indicator as original array values 
                      only in  PEAK/VALLEY indexes 
v0.3.4, 2025-11-25 -- zigzag now return lines
                      original code return only values in peak points, keeps NAN
                      as values in between PEAK and VALLEY. Added linear 
                      interpolation call feom pandas package to fill up array.
v0.3.5, 2025-12-00 -- calc next drawdown and gain and time  for each value
V0.3.6, 2025-12-01 -- fixing errors  in calculations
V0.3.7, 2026-02-12 -- update test_core and change 'zigzag' func to the same 
                      level down_thresh as up_thresh. change check for type as
                      Pandas Series for array in inner function __to_ndarray
                      change compute_perfomance_nd
V0.3.8, 2026-03-11 -- update test_core: all function accept threshold as 
                      fraction part of it. identified core incorrectly calc 
                      pivots for negative negaive values. update test_core 
                      functions.  
                      'compute_performance' base on parameters type as pd.Series
                      'compute_performance_nd' based on parameters type ndarray 
                      zigzag  - removed not implemented params 'max_bars' and 
                      'max_time', but  add 'down_rate' as separate threshold 
                      value. if missed  down_rate = -min_rate.
                      implemented 'linear' or 'time' interpolation if pd.Series 
                      has 'time' index. Use 'linear' only for ndarray vectors 
